<div class="container">
  <h2 class="form-title">新規登録</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { multipart: true, class: "registration-form" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <!-- Name field -->
    <div class="form-group">
      <%= f.label :name, "名前", class: "form-label" %>
      <%= f.text_field :name, class: "form-input", placeholder: "名前を入力", required: true %>
    </div>

    <div class="form-group">
      <%= f.label :email, "メールアドレス", class: "form-label" %>
      <%= f.email_field :email, class: "form-input", placeholder: "メールアドレスを入力", required: true %>
    </div>

    <div class="form-group">
      <%= f.label :password, "パスワード", class: "form-label" %>
      <%= f.password_field :password, class: "form-input", placeholder: "パスワードを入力", required: true %>
    </div>

    <div class="form-group">
      <%= f.label :password_confirmation, "確認用パスワード", class: "form-label" %>
      <%= f.password_field :password_confirmation, class: "form-input", placeholder: "確認用パスワードを入力", required: true %>
    </div>

    <div class="actions">
      <%= f.submit "新しいアカウントを作成", class: "form-submit-button" %>
    </div>
  <% end %>

  <div class="login-link">
    <%= link_to "アカウントをお持ちの方はログイン", new_session_path(resource_name) %>
  </div>
</div>
